update:
  cooldown_days: 7
ci:
  autoupdate_commit_msg: "chore(deps): update pre-commit hooks"
  autofix_commit_msg: "style: pre-commit fixes"
  autoupdate_schedule: "quarterly"
  skip:
    - uv-lock
    - pip-compile
    - sync-pyproject-min-versions
default_install_hook_types:
  - pre-commit
  - commit-msg
repos:
  # * meta hooks
  # - repo: meta
  #   hooks:
  #     - id: check-hooks-apply
  #     - id: check-useless-excludes

  # * sync dependencies
  - repo: https://github.com/wpk-nist-gov/sync-pre-commit-hooks
    rev: v0.10.0
    hooks:
      - id: forbidden-files
      - id: sync-pre-commit-deps
      - id: sync-uv-dependency-groups

        # update cookiecutter
      - id: sync-pre-commit-deps
        args:
          - "--config"
          - "{{cookiecutter.project_name}}/.pre-commit-config.yaml"

      # sync min versions
      - id: sync-pyproject-min-versions
        files: |
          (?x)^({{cookiecutter.project_name}}/)?
          (
            pyproject\.toml|
            requirements/lock/uvx-tools\.txt|
            noxfile\.py|
            ^tools/.*\.py
          )$
        args:
          - "--requirements=requirements/lock/uvx-tools.txt"

  # * Top level
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-added-large-files
        exclude: ^uv[.]lock
      - id: check-case-conflict
      - id: check-merge-conflict
        exclude: &exclude_template "^{{cookiecutter.project_name}}"
        args: [--assume-in-merge]
      - id: check-yaml
        exclude: *exclude_template
      - id: check-json
        exclude: *exclude_template
      - id: check-toml
        exclude: *exclude_template
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: mixed-line-ending

  # * toml/pyproject.toml
  # ** format
  - repo: https://github.com/ComPWA/taplo-pre-commit
    rev: v0.9.3
    hooks:
      - id: taplo-format
        exclude: ^{{cookiecutter.project_name}}|(^|/)pyproject\.toml$
      - id: taplo-lint
        exclude: *exclude_template
        args: ["--no-schema"]
  - repo: https://github.com/tox-dev/pyproject-fmt
    rev: v2.28.1
    hooks:
      - id: pyproject-fmt
        exclude: *exclude_template
  # ** validate (schema-store)
  - repo: https://github.com/henryiii/validate-pyproject-schema-store
    rev: 2026.08.15
    hooks:
      - id: validate-pyproject
        name: validate-pyproject-schema-store
        stages: [manual]
  # ** repo review
  - repo: https://github.com/scientific-python/cookie
    rev: 2026.08.14
    hooks:
      - id: sp-repo-review
        stages: [manual]

  # * Prettier
  - repo: https://github.com/rbubley/mirrors-prettier
    rev: v3.9.6
    hooks:
      - id: prettier
        exclude: *exclude_template
        alias: markdownlint
        stages: [pre-commit, manual]
        args:
          - "--cache"
          - "--cache-location=.prettier_cache/cache"

  # * Markdown
  - repo: https://github.com/DavidAnson/markdownlint-cli2
    rev: v0.23.2
    hooks:
      - id: markdownlint-cli2
        exclude: *exclude_template
        alias: markdownlint
        args: [--style, prettier]

  # Just
  - repo: https://github.com/wpk-nist-gov/just-pre-commit
    rev: v1.58.0
    hooks:
      - id: justfile-format
        exclude: *exclude_template

  # * Linting
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.4
    hooks:
      - id: ruff-check
        exclude: *exclude_template
        alias: ruff
        args: [--fix, --show-fixes]
        types_or: &ruff_types [python, pyi, jupyter, markdown]
      - id: ruff-check
        alias: ruff-check-config
        name: ruff check config
        args: [--fix, --show-fixes]
        types_or: [file]
        files: ^pyproject\.toml$|^\.?ruff\.toml$
      - id: ruff-format
        exclude: *exclude_template
        alias: ruff
        types_or: *ruff_types

  # * Spelling
  # ** typos
  - repo: https://github.com/adhtruong/mirrors-typos
    rev: v1.49.0
    hooks:
      - id: typos
        exclude_types: [jupyter]
        exclude: ^uv[.]lock$|^requirements|^\.copier-answers\.ya?ml$|^{{cookiecutter.project_name}}

  # * Dependencies
  # ** pyproject2conda
  - repo: https://github.com/usnistgov/pyproject2conda
    rev: v0.23.2
    hooks:
      - id: pyproject2conda-project
        alias: requirements
        args:
          - "--custom-command=prek run pyproject2conda-project --all-files"
  # ** uv
  - repo: https://github.com/astral-sh/uv-pre-commit
    rev: 0.12.6
    hooks:
      - id: uv-lock
        alias: requirements
        args: [--locked]
      - id: pip-compile
        alias: requirements
        entry: python tools/uv_locker.py
        pass_filenames: true
        files: ^requirements/[^/]*\.txt$|^tools/*\.py$

  # * Commit message
  - repo: https://github.com/commitizen-tools/commitizen
    rev: v4.18.0
    hooks:
      - id: commitizen
        stages: [commit-msg]

  # * Cog
  - repo: local
    hooks:
      - id: cog-copier-yaml
        name: cog-copier-yaml
        alias: cog
        entry: uv run tools/cog_runner.py --format=prettier -- copier.yaml
        language: python
        stages: [manual]
        pass_filenames: false

  # * Actions
  - repo: https://github.com/python-jsonschema/check-jsonschema
    rev: 0.38.0
    hooks:
      - id: check-github-workflows
        alias: actions
        priority: 0
      - id: check-github-actions
        alias: actions
  - repo: https://github.com/rhysd/actionlint
    rev: v1.7.12
    hooks:
      - id: actionlint
        alias: actions
        stages: [manual]
        args:
          - "-ignore=SC2129" # ignorable stylistic lint from shellcheck
          - "-ignore=SC2016" # another shellcheck lint: seems to have false positives?
        language: golang # means renovate will also update `additional_dependencies`
        additional_dependencies:
          # actionlint has a shellcheck integration which extracts shell scripts in `run:` steps from GitHub Actions
          # and checks these with shellcheck. This is arguably its most useful feature,
          # but the integration only works if shellcheck is installed
          - "github.com/wasilibs/go-shellcheck/cmd/shellcheck@v0.11.1"
        # see https://github.com/rhysd/actionlint/issues/46
        exclude: ^\.github/actions
        priority: 0
  - repo: https://github.com/zizmorcore/zizmor-pre-commit
    # Zizmor version.
    rev: v1.29.0
    hooks:
      # Run the linter.
      - id: zizmor
        exclude: *exclude_template
        alias: actions

  # * Other tools --------------------------------------------------------------
  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: v0.11.0.1-1
    hooks:
      - id: shellcheck
